# TODO - Stock update -> Dashboard products reflection

- [ ] Fix Dashboard Inventory "Save" to persist stock to backend (use correct API base URL) and refresh in-memory products.
- [ ] Ensure updated stock also reflects immediately in the dashboard product modal and any other inventory-derived sections.
- [ ] Verify backend PATCH /api/inventory/:id updates `products.stock` and GET /api/inventory returns updated stock.
- [ ] Run frontend + backend dev checks (manual).

